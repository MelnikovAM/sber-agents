# Перезапуск бота после установки зависимостей

## ✅ Пакет einops установлен

Установлен пакет `einops==0.8.1`, который требуется для модели `nomic-ai/nomic-embed-text-v1.5`.

## 🚀 Перезапустите бота

### В текущем терминале:

1. **Остановите бота:** Нажмите `Ctrl+C`

2. **Запустите снова:**
   ```bash
   make run
   ```

### Или откройте новый терминал:

```bash
cd /Users/Aleksei/sber-agents/06-monitoring-qa/live
make run
```

## ✅ Что должно произойти при успешном запуске

```
✅ Bot starting...
✅ Starting indexing...
✅ Starting full reindexing...
✅ Found 2 PDF files in data
✅ Loaded ouk_potrebitelskiy_kredit_lph.pdf
✅ Loaded usl_r_vkladov.pdf
✅ Split into 377 chunks
✅ Loaded 212 Q&A pairs from JSON
✅ Total chunks to index: 589
✅ Load pretrained SentenceTransformer: nomic-ai/nomic-embed-text-v1.5
✅ Using HuggingFace embeddings: nomic-ai/nomic-embed-text-v1.5
✅ Created vector store with 589 chunks
✅ Indexing completed successfully: 589 documents indexed
✅ Starting bot polling...
```

## 📱 Проверка в Telegram

После успешного запуска проверьте:

### 1. Проверьте статус
```
/index_status
```

Должно показать:
```
📊 Статус индексации:
Статус: initialized
Количество документов: 589
```

### 2. Задайте тестовый вопрос
```
Какие условия по вкладам?
```

Должны получить ответ на основе документов!

### 3. Попробуйте уточняющий вопрос
```
А какие проценты?
```

Бот должен понимать контекст и отвечать про проценты по вкладам.

## ⚠️ Если возникли ошибки

### Ошибка: "No module named 'einops'"

**Решение:** Убедитесь, что бот перезапущен после `uv sync`

### Ошибка: "trust_remote_code"

**Решение:** Уже исправлено в коде, просто перезапустите бота

### Ошибка: "401 Authentication"

**Решение:** Проверьте `.env` файл:
```bash
cat .env | grep OPENAI_API_KEY
```

Должно быть:
```
OPENAI_API_KEY=fw_XXXXXXXXXXXXXXXX
```

### Ошибка: "Model not found"

**Решение:** Проверьте MODEL в `.env`:
```bash
MODEL=accounts/fireworks/models/llama-v3p3-70b-instruct
```

## 💡 Все исправления применены

✅ Добавлен `trust_remote_code=True` в `indexer.py` и `evaluation.py`  
✅ Установлен пакет `einops==0.8.1`  
✅ Обновлены модели Fireworks AI  
✅ Настроены локальные embeddings  

**Осталось только перезапустить бота!** 🚀

